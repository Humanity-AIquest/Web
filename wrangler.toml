name = "staging"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"

# D1 database binding (humanity_ai_db_staging) and secrets (ANTHROPIC_API_KEY)
# are configured directly in the Cloudflare dashboard for this Worker, not here.
